# API Reference

Auto-generated from docstrings in the `bindwood` package via [mkdocstrings](https://mkdocstrings.github.io/).

## Modules

| Module | Purpose |
|--------|---------|
| [`bindwood.cli`](cli.md) | Command-line entry point (`bindwood`). |
| [`bindwood.scan`](scan.md) | Orchestrates the full scan pipeline. |
| [`bindwood.db`](db.md) | SQLite schema, loaders, and embeddings helpers. |
| [`bindwood.ddl`](ddl.md) | SQL DDL extractor (`sqlglot`). |
| [`bindwood.jsts`](jsts.md) | JavaScript / TypeScript extractor (tree-sitter). |
| [`bindwood.python`](python.md) | Python extractor (tree-sitter). |
| [`bindwood.servers`](servers.md) | MCP and HTTP servers. |

!!! note
    If a page looks empty, the underlying module may not yet have docstrings. The pages below will populate automatically as docstrings are added.
